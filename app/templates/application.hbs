<div class="container">
	<div class="row">
		<div class="col-md-4">
			<h2>Welcome to EmberNote</h2>
		</div>
		<div class="col-md-8">
		{{#if user}}
			Hello {{user.name}}
		{{/if}}

		{{#unless user}}
			{{#link-to 'register'}}Register{{/link-to}} &nbsp;
			{{#link-to 'login'}}Login{{/link-to}} &nbsp;
		{{/unless}}
		{{#link-to 'employeetypes'}}Employee Types{{/link-to}}
		</div>
	</div>
	<div class="row">
		<div>
			{{outlet}}
		</div>
	</div>
</div>